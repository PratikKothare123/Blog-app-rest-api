{
  "name": "blog_page",
  "version": "1.0.0",
  "description": "A simple blog application built with HTML, CSS, and JavaScript using `json-server` to simulate a backend API. Perform full CRUD operations (Create, Read, Update, Delete) via REST API.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Pratik Kothare",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.1.10",
    "express": "^5.1.0",
    "method-override": "^3.0.0",
    "uuid": "^11.1.0"
  }
}
